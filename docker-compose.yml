services:
  app:
    build: .
    container_name: hello-app
    # No ports needed - only nginx will talk to it
    
  web:
    image: nginx:alpine
    container_name: hello-web
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app