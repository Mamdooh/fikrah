version: '3.8'

services:
  # Your Python application
  app:
    build: .
    container_name: hello-app
    
  # Nginx web server (second service)
  web:
    image: nginx:alpine
    container_name: hello-web
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro